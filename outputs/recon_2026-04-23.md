# Recon Notes — 2026-04-23

## Scope

Stage 0 Task P0.0 format reconnaissance for:

1. module CSV schema
2. module Pattern chunk split behavior
3. `pouch_SOH` raw vs `step` difference

## 1. Module CSV columns

- Checked file:
  - `모듈엑셀파일\모듈엑셀파일\모듈데이터 엑셀파일(2022.01~2023.07)\CH1\RPT\2023_01_12_에기연_열화모듈_CYCLE_25deg_2000cycle_RPT_CH01.csv`
- Result:
  - Header width is `90` columns.
  - Base electrical columns are already module-scale units:
    - `Current(A)`
    - `Capacity(Ah)`
    - `Power(W)`
    - `wattHour(Wh)`
  - Core metadata columns match the cell schema family:
    - `State`, `Voltage(V)`, `StepNo`, `TotTime`, `CurCycle`, `TotCycle`
  - Module-specific columns include:
    - `3` aux temperature channels:
      - `[001]AuxT...`
      - `[002]AuxT...`
      - `[003]AuxT...`
    - `14` cell-voltage-like channels:
      - `[004]AuxV...` through `[017]AuxV...`
    - aggregate diagnostics:
      - `Aux Temp. Max/Min/Dev.`
      - `Aux Volt. Max/Min/Dev.`
      - `Capacitance(F)`
- Implication:
  - `algo.data_io` needs a module-only alias layer in `kier.py`.
  - Cell loader assumptions about `mA/mAh` must not be applied to module CSV.

## 2. Module Pattern split

- Checked folder:
  - `모듈엑셀파일\모듈엑셀파일\모듈데이터 엑셀파일(2022.01~2023.07)\CH1\Pattern\2023_01_08_25deg_2000cycle`
- Files found:
  - `...CH01.csv`
  - `...CH01(0).csv`
  - `...CH01(1).csv`
  - `...CH01(2).csv`
- Boundary check:
  - `CH01.csv` last row: `StepNo=15`, `TotTime=D0 00:15:24.00`
  - `CH01(0).csv` first row: `StepNo=15`, `TotTime=D0 00:15:25.00`
  - `CH01(0).csv` last row: `StepNo=13`, `TotTime=D1 00:13:46.00`
  - `CH01(1).csv` first row: `StepNo=13`, `TotTime=D1 00:13:47.00`
  - `CH01(1).csv` last row: `StepNo=11`, `TotTime=D2 00:12:01.00`
  - `CH01(2).csv` first row: `StepNo=11`, `TotTime=D2 00:12:02.00`
- Result:
  - Chunk order is `CH01.csv -> CH01(0) -> CH01(1) -> CH01(2)`.
  - `TotTime` is already continuous at chunk boundaries with `+1 s` progression.
  - Boundary `StepNo` stays identical across adjacent files, which means the split happens inside an active step.
- Implication:
  - Merge logic can concatenate files in filename order without synthetic time offsets.
  - Loader should treat `CH01.csv` as the first canonical chunk and append numbered chunks after it.

## 3. pouch_SOH raw vs step

- Checked pair:
  - raw:
    - `02_실험_데이터\pouch_SOH\pattern_MG_data\25도\MG_220425_MG_10_ch1_M01Ch001(001).csv`
  - step:
    - `02_실험_데이터\pouch_SOH\pattern_MG_data\25도step\MG_220425_MG_10_ch1_M01Ch001(001)_step.csv`
- Result:
  - Raw file line count: `114,428`
  - Step file line count: `22`
  - Raw file is 1 Hz-like time series with columns such as:
    - `DataSequence`, `StepNo`, `TotTime(Min)`, `Voltage(V)`, `Current(A)`, `Capacity(Ah)`
  - Step file is a step-aggregated summary table with columns such as:
    - `시작시간`, `종료시간`, `총시간(Min)`, `평균전압(V)`, `평균전류(A)`, `IR(mΩ)`
- Implication:
  - `25도step/` is not just a raw split copy.
  - It is a step-summary derivative and must not be cataloged as equivalent raw time series.
  - Canonical source for loader/catalog should remain `25도/` and `40도/`.

## Extra note — `최근셀데이터` vs `셀 엑셀파일\셀 엑셀파일`

- Spot-checked matching run:
  - `최근셀데이터\23_01_12_에기연 열화셀_25deg_1400cycle_RPT_ch2\...`
  - `셀 엑셀파일\셀 엑셀파일\CH2_25deg\RPT\23_01_12_에기연 열화셀_25deg_1400cycle_RPT_ch2_M01Ch002(002).csv`
- Observations:
  - `셀 엑셀파일` side contains the analysis-ready `1 Hz` CSV with `42,433` lines.
  - The matched `최근셀데이터` `.txt` sample for the same run was only a `2-line` summary, alongside `.cts/.cyc/.ini/.log/.sch` and `StepEnd.csv`.
  - `최근셀데이터` also contains extra low-temperature runs (`5/10/15 °C`) that are not organized into the current `셀 엑셀파일\셀 엑셀파일` tree.
- Working interpretation:
  - `최근셀데이터` looks like the broader run-folder archive or source-side experiment container.
  - `셀 엑셀파일\셀 엑셀파일` looks like the curated, analysis-ready CSV export set for the main loader path.
  - For overlapping `25/50 °C` runs, `셀 엑셀파일\셀 엑셀파일` is the safer canonical input for Stage 0 loaders.
  - `최근셀데이터` should still be treated as valuable source evidence, especially for newly discovered `5/10/15 °C` conditions and for provenance tracing.
